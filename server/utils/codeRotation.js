module.exports = {
  async codeRotation() {
    //get the date and check if there is a change,
    //store the current day inside of local storage and on component load of the codewars component check the current day is === to the date it storage
    //if theres a change display a new challenge by talking to the backend and grabbing the storages array of codeword objects
  },
};
